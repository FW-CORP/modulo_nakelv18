<odoo>
    <data>
        <template id="custom_logout_text" inherit_id="point_of_sale.pos_template" priority="99">
            <xpath expr="//button[contains(@class, 'select_logout')]" position="attributes">
                <attribute name="innerHTML">
                    <![CDATA[
                    <span class="d-flex flex-grow-1 align-items-center">Salir / Cerrar Sesión</span>
                    ]]>
                </attribute>
            </xpath>
        </template>
    </data>
</odoo>